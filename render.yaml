services:
  - type: web
      name: social-publisher
          env: python
              plan: free
                  region: frankfurt
                      buildCommand: "pip install flask"
                          startCommand: python server.py
                              autoDeploy: true
