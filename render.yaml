services:
  - type: web
      name: social-publisher
          env: python
              plan: free
                  region: frankfurt
                      buildCommand: "echo build done"
                          startCommand: python server.py
                              autoDeploy: true
